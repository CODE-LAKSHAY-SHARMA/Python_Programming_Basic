"""
TUPLES and List difference 
1. tuples are immutable , whereas list are mutable
"""

my_tuple = (1, 2, 3, 4, "lakshay", True, False)
print(my_tuple)
print(id(my_tuple))

# tuples once created cannot be changed
# my_tuple[0] = 100  # give error
# print(my_tuple)

# ADD DELETE UPDATE CANNOT BE DONE IN TUPLES

# WHAT IS THE USECASE OF TUPLE
#  when we want to store a collection of items and we don't want to change
